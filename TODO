1) Need to add KEYWORDS or token as class KEYWORDS as constant is limiting
2) if list( match for the rest till ) also check for parametrs have commas or not.
3) Need to see Visitor pattern for fetching token
